import { CategoryFormatterPipePipe } from './category-formatter-pipe.pipe';

describe('CategoryFormatterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryFormatterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
